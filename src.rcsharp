#include("./src_base_structs.rcsharp")
#include("./lexer.rcsharp")
#include("./parser_expression.rcsharp")
#include("./parser_type.rcsharp")
#include("./parser.rcsharp")
fn main(){
    rcsharp_compile("D:/Projects/rcsharp/.code_snippets/compier_tests/hello_world/test.rcsharp", "D:/Projects/rcsharp/output_v2.ll");
}
enum SymbolTableTag{ 
    Function = 0,
    GenericFunction = 1,
    Struct = 2,
    GenericStruct = 3,
    Enum = 4,
    Static = 5,
    Constant = 6,
    OnlyTypesMask = 7,

    Solved = 8, // Flags
}
struct SymbolTableEntry { tag: SymbolTableTag, path: PathEx, data_index: u32 }

struct EnumDefinedField    { name: u16, value: u64 }
struct EnumSymbolTableEntry { representative_type: comp_type::CompilerType, fields: vector::Vec<EnumDefinedField>, flags: u8 }

struct StructDefinedField    { name: u16, type: comp_type::CompilerType }
struct StructSymbolTableEntry { fields: vector::Vec<StructDefinedField>, layout: layout::Layout, llvm_signature: string::String, flags: u8 }

struct FunctionSymbolTableEntry { arguments: vector::Vec<StructDefinedField>, return_type: comp_type::CompilerType, body: vector::Vec<Stmt>, llvm_signature: string::String, flags: u8 }
struct GenericFunctionSymbolTableEntry { arguments: vector::Vec<GenericStructDefinedField>, is_generic_return: bool, return_type: comp_type::CompilerType, body: vector::Vec<Stmt>, generic_tokens: vector::Vec<u16>, implementations: vector::Vec<Implementation>, llvm_signature_base: string::String, flags: u8 }

struct Implementation { type: vector::Vec<comp_type::CompilerType>, llvm_signature: string::String, layout: layout::Layout }

struct GenericStructDefinedField    { name: u16, type: comp_type::CompilerType, is_generic: bool }
struct GenericStructSymbolTableEntry { fields: vector::Vec<GenericStructDefinedField>, generic_tokens: vector::Vec<u16>, implementations: vector::Vec<Implementation>, llvm_signature_base: string::String, flags: u8 }

struct ConstantSymbolTableEntry { type: comp_type::CompilerType, defined_value: rvalue::Rvalue }
struct StaticSymbolTableEntry { type: comp_type::CompilerType, has_initialized_value: bool, initialized_value: rvalue::Rvalue }

struct SymbolTable{
    symbols: vector::Vec<SymbolTableEntry>,
    symbol_vector: &vector::Vec<string::String>,
    constant_strings_used: vector::Vec<u32>,

    functions: vector::Vec<FunctionSymbolTableEntry>,
    generic_functions: vector::Vec<GenericFunctionSymbolTableEntry>,
    structs: vector::Vec<StructSymbolTableEntry>,
    generic_structs: vector::Vec<GenericStructSymbolTableEntry>,
    enums: vector::Vec<EnumSymbolTableEntry>,
    constants: vector::Vec<ConstantSymbolTableEntry>,
    statics: vector::Vec<StaticSymbolTableEntry>,
}
fn insert_symbol_into_table(table: &SymbolTable, entry: SymbolTableEntry){
    let entry: SymbolTableEntry = entry;
    if get_symbol_from_table(table, entry.path) != null{
        process::throw("Duplicate symbols detected");
    }
    vector::push::<SymbolTableEntry>(&table.symbols, entry);
}
fn get_symbol_from_table(table: &SymbolTable, path: PathEx): &SymbolTableEntry{
    let path: PathEx = path;
    if path.len == 0 { process::throw("Path with length zero"); }

    const entries: &SymbolTableEntry = table.symbols.array;
    const entries_len: u32 = table.symbols.length;
    for i in 0 as u32 .. entries_len{ // TODO Handle enums differently
        const entry: &SymbolTableEntry = &entries[i];
        const entry_path_len: u8 = entry.path.len;
        if entry_path_len != path.len{
            continue;
        }
        if entry.path.data[0] != path.data[0]{
            continue;
        }
        let same: bool = true;
        for j in 1 as u8 .. entry_path_len{ 
            if entry.path.data[j] != path.data[j]{
                same = false;
                break;
            }
        }
        if !same{
            continue;
        }
        return entry;
    }
    return null;
}

fn rcsharp_compile(input_file_path: &i8, output_file_path: &i8) {
    const buffer: &i8 = stalloc::<i8>(255);
    let file_name_start: &i8;
    const len: i32 = fs::GetFullPathNameA(input_file_path, 255, buffer, bitcast::<&i8>(&file_name_start));
    for i in 0 as i32 .. len {
        let c_ptr: &i8 = bitcast::<&i8>(bitcast::<usize>(buffer) + (i as usize));
        if *c_ptr == 47 as i8 {
            *c_ptr = 92 as i8;
        }
    }
    const env_path_len: i32 = (bitcast::<usize>(file_name_start) - bitcast::<usize>(buffer)) as i32;
    console::println_i64(env_path_len as i64);
    // Main script environment path
    // for D:/Projects/rcsharp/src.rcsharp AND ./src.rcsharp gives D:/Projects/rcsharp/
    let base_env : string::String = string::from_data(buffer, env_path_len);
    console::write_string(&base_env);
    string::free(&base_env); 

    let file_stack: vector::Vec<string::String> = vector::new::<string::String>();
    vector::push::<string::String>(&file_stack, string::from_c_string(buffer));
    


    console::writeln("Loading...", 10);
    let main_statement_vector: vector::Vec<Stmt> = vector::new::<Stmt>();
    let symbol_vector: vector::Vec<string::String> = vector::new::<string::String>();
    let file_index: u32 = 0;
    while file_index < file_stack.length{
        console::write("Reading File\t", 13);
        console::write_string(&file_stack.array[file_index]);
        console::write("\n", 1);
        let file_data: string::String = fs::read_full_file_as_string_string(&file_stack.array[file_index]);
        let token_vector: vector::Vec<TokenData> = vector::new::<TokenData>();
        lex(&file_data, &token_vector, &symbol_vector);
        string::free(&file_data);
        let statement_vector: vector::Vec<Stmt> = vector::new::<Stmt>();
        parse(token_vector.array, token_vector.length, &symbol_vector, &statement_vector);
        

        for idx in 0 as u32 .. statement_vector.length{
            const stmt: &Stmt = &statement_vector.array[idx];
            if stmt.statement_type == StatementType::CompilerHint{
                const node: &HintNode = stmt.node_ptr as &HintNode;
                const sym_array: &string::String = symbol_vector.array;
                if sym_array[node.name].len == 7 && mem::compare(sym_array[node.name].data, "include", 7) == 0{
                    if node.expressions.length != 1{
                        process::throw("include directive must have only one path");
                    }
                    const expr: &Expression = &node.expressions.array[0];
                    if expr.type != ExpressionType::StringConst{
                        process::throw("include directive must have only one path");
                    }
                    const str_expr: &StringConstExpr = expr.val as &StringConstExpr;

                    const inc_path_sym: &string::String = &sym_array[str_expr.idx];
                    const sym_c_str: &i8 = stalloc::<i8>((inc_path_sym.len + 1) as i64);
                    string::as_c_string_stalloc(inc_path_sym, sym_c_str);

                    const inc_buffer: &i8 = stalloc::<i8>(255);
                    let inc_file_name_start: &i8;
                    fs::GetFullPathNameA(sym_c_str, 255, inc_buffer, bitcast::<&i8>(&inc_file_name_start));
                    let abs_inc_path: string::String = string::from_c_string(inc_buffer);


                    let found: bool = false;
                    for i in 0 as u32 .. file_stack.length{
                        if string::equal(&abs_inc_path, &file_stack.array[i]){
                            found = true;
                            break;
                        }
                    }
                    if !found{
                        console::write("Found include\t", 14);
                        console::write_string(&abs_inc_path);
                        console::write("\n\r", 2);
                        vector::push::<string::String>(&file_stack, abs_inc_path);
                    } else {
                        string::free(&abs_inc_path); 
                    }
                    
                    continue;
                }else{
                    process::throw("Unrecognized directive");
                }
            }
            vector::push::<Stmt>(&main_statement_vector, *stmt);
        }
        vector::free::<Stmt>(&statement_vector);
        file_index = file_index + 1;
    }

    console::writeln("Loaded...", 9);
    console::writeln("Project Files...", 16);
    console::write("Sym Vec Len: ", 13);
    console::println_u64(symbol_vector.length as u64);
    for i in 0 as u32 .. file_stack.length{
        console::write("\t", 1);
        console::write_string(&file_stack.array[i]);
        console::write("\n", 1);
    }
    console::println_i64(main_statement_vector.length as i64);
    console::writeln("Compiling...", 12);
    let stdout: string::String = string::empty();
    compile(&main_statement_vector, &symbol_vector, &stdout);

    const out_buffer: &i8 = stalloc::<i8>(255);
    let out_file_name_start: &i8;
    const out_len: i32 = fs::GetFullPathNameA(output_file_path, 255, out_buffer, bitcast::<&i8>(&out_file_name_start));
    
    for i in 0 as i32 .. out_len {
        let c_ptr: &i8 = bitcast::<&i8>(bitcast::<usize>(out_buffer) + (i as usize));
        if *c_ptr == 47 as i8 {
            *c_ptr = 92 as i8;
        }
    }

    fs::write_to_file(out_buffer, stdout.data, stdout.len as u32);

    //console::write("\t---\n", 5);
    //console::write_string(&stdout);
    //console::write("\n", 1);
    process::ExitProcess(0);
}
fn debug_dump_type(expr: &Type, symbol_vector: &vector::Vec<string::String>, stdout: &string::String) {
    
}
fn debug_dump_expression(expr: &Expression, symbol_vector: &vector::Vec<string::String>, stdout: &string::String) {

    if expr.type == ExpressionType::StaticAccess{
        const data: &StaticAccessExpr = expr.val as &StaticAccessExpr;
        debug_dump_expression(&data.rval, symbol_vector, stdout);
        string::append_str(stdout, "::", 2);
        string::append_str(stdout, symbol_vector.array[data.member].data, symbol_vector.array[data.member].len);
        return;
    }
    if expr.type == ExpressionType::MemberAccess{
        const data: &MemberAccessExpr = expr.val as &MemberAccessExpr;
        debug_dump_expression(&data.rval, symbol_vector, stdout);
        string::append(stdout, '.');
        string::append_str(stdout, symbol_vector.array[data.member].data, symbol_vector.array[data.member].len);
        return;
    }
    if expr.type == ExpressionType::Cast{
        const data: &CastExpr = expr.val as &CastExpr;
        debug_dump_expression(&data.rval, symbol_vector, stdout);
        string::append_str(stdout, " as ", 4);
        debug_dump_type(&data.to, symbol_vector, stdout);
        return;
    }
    if expr.type == ExpressionType::UnaryOp{
        const data: &UnaryOpExpr = expr.val as &UnaryOpExpr;
        if data.op == UnaryOp::Negate{
            string::append(stdout, '-');
        }else if data.op == UnaryOp::Not{
            string::append(stdout, '!');
        }else if data.op == UnaryOp::Deref{
            string::append(stdout, '*');
        }else if data.op == UnaryOp::Pointer{
            string::append(stdout, '&');
        }else if data.op == UnaryOp::BitFlip{
            string::append(stdout, '~');
        }
        debug_dump_expression(&data.rval, symbol_vector, stdout);
        return;
    }
    if expr.type == ExpressionType::Range{
        const data: &RangeExpr = expr.val as &RangeExpr;
        debug_dump_expression(&data.start, symbol_vector, stdout);
        string::append_str(stdout, "..", 2);
        if data.inclusive{
            string::append(stdout, '=');
        }
        debug_dump_expression(&data.end, symbol_vector, stdout);
        return;
    }
    if expr.type == ExpressionType::Index{
        const data: &IndexExpr = expr.val as &IndexExpr;
        debug_dump_expression(&data.lval, symbol_vector, stdout);
        string::append(stdout, '[');
        debug_dump_expression(&data.index_expr, symbol_vector, stdout);
        string::append(stdout, ']');
        return;
    }
    if expr.type == ExpressionType::NameWithGenerics{
        const data: &NameWithGenericsExpr = expr.val as &NameWithGenericsExpr;
        debug_dump_expression(&data.rval, symbol_vector, stdout);
        string::append(stdout, ':');
        string::append(stdout, ':');
        string::append(stdout, '<');
        for i in 0 as u32 .. data.generics.length{
            debug_dump_type(&data.generics.array[i], symbol_vector, stdout);
            if i != data.generics.length - 1{
                string::append_str(stdout, ", ", 2);
            }
        }
        string::append(stdout, '>');
        return;
    }
    if expr.type == ExpressionType::StructInit{
        const data: &StructInitExpr = expr.val as &StructInitExpr;
        debug_dump_expression(&data.type, symbol_vector, stdout);
        string::append(stdout, ':');
        string::append(stdout, ':');
        string::append(stdout, '{');
        for i in 0 as u32 .. data.expr.length{
            string::append_str(stdout, symbol_vector.array[data.expr.array[i].name].data, symbol_vector.array[data.expr.array[i].name].len);
            string::append_str(stdout, ": ", 2);
            debug_dump_expression(&data.expr.array[i].expr, symbol_vector, stdout);
            if i != data.expr.length - 1{
                string::append_str(stdout, ", ", 2);
            }
        }
        string::append(stdout, '}');
        return;
    }
    console::println_i64(expr.type as i64);
}
fn compile_internal_sym_table_prefill(path: &Path, statement_vector: &vector::Vec<Stmt>, symbol_table: &SymbolTable, stdout: &string::String) {
    let index: u32 = 0; 
    let len: u32 = statement_vector.length;
    const symbol_vector: &vector::Vec<string::String> = symbol_table.symbol_vector;
    const sym_array: &string::String = symbol_vector.array;
    
    while index < len {
        let statement: &Stmt = &statement_vector.array[index];
        if statement.statement_type == StatementType::CompilerHint{
            process::throw("INCLUDE ALREADY HANDLED, OTHERS ARE NOT IMPLEMENTED");
        } else if statement.statement_type == StatementType::Attribute{
            // Skip.
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Function{
            const node: &FnNode = statement.node_ptr as &FnNode;

            let struct_path: PathEx = path_to_path_ex_name(path, node.name);
            let sym_t_entry: SymbolTableEntry;
            sym_t_entry.path = struct_path;
            sym_t_entry.data_index = 4294967295 as u32;
            if node.generic.length != 0{
                sym_t_entry.tag = SymbolTableTag::GenericFunction;
            }else{
                sym_t_entry.tag = SymbolTableTag::Function;
            }
            insert_symbol_into_table(symbol_table, sym_t_entry);
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Struct{ // Done
            const node: &StructNode = statement.node_ptr as &StructNode;

            let struct_path: PathEx = path_to_path_ex_name(path, node.name);
            let sym_t_entry: SymbolTableEntry;
            sym_t_entry.path = struct_path;
            if node.generic.length != 0{
                sym_t_entry.tag = SymbolTableTag::GenericStruct;
                let llvm_signature: string::String = string::from_c_string("struct.");
                append_path_ex(&sym_t_entry.path, symbol_table.symbol_vector.array, &llvm_signature);
                let sym_entry: GenericStructSymbolTableEntry = GenericStructSymbolTableEntry::{
                    flags: node.flags,
                    fields: vector::new::<GenericStructDefinedField>(),
                    implementations: vector::new::<Implementation>(),
                    generic_tokens: vector::new::<u16>(),
                    llvm_signature_base: llvm_signature
                };
                for x in 0 as u32 .. node.generic.length{
                    vector::push::<u16>(&sym_entry.generic_tokens, node.generic.array[x]);
                }
                vector::push::<GenericStructSymbolTableEntry>(&symbol_table.generic_structs, sym_entry);
                sym_t_entry.data_index = symbol_table.generic_structs.length - 1;
            }else{
                sym_t_entry.tag = SymbolTableTag::Struct;
                let llvm_signature: string::String = string::from_c_string("struct.");
                append_path_ex(&sym_t_entry.path, symbol_table.symbol_vector.array, &llvm_signature);
                let sym_entry: StructSymbolTableEntry = StructSymbolTableEntry::{
                    flags: node.flags,
                    fields: vector::new::<StructDefinedField>(),
                    layout: layout::invalid(),
                    llvm_signature: llvm_signature
                };
                vector::push::<StructSymbolTableEntry>(&symbol_table.structs, sym_entry);
                sym_t_entry.data_index = symbol_table.structs.length - 1;
            }
            insert_symbol_into_table(symbol_table, sym_t_entry);
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Enum{
            const node: &EnumNode = statement.node_ptr as &EnumNode;
            
            let enum_path: PathEx = path_to_path_ex_name(path, node.name);
            let sym_t_entry: SymbolTableEntry;
            sym_t_entry.path = enum_path;
            sym_t_entry.tag = SymbolTableTag::Enum;
            sym_t_entry.data_index = 4294967295 as u32; 
            insert_symbol_into_table(symbol_table, sym_t_entry);
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Namespace{
            const node: &NamespaceNode = statement.node_ptr as &NamespaceNode;
            let path: Path = *path;
            if path.len == 8{
                process::throw("Nested namespaces too deep");
            }
            path.data[path.len] = node.name;
            path.len = path.len + 1;
            compile_internal_sym_table_prefill(&path, &node.nodes, symbol_table, stdout);
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::VarDec{
            const node: &DeclarationNode = statement.node_ptr as &DeclarationNode;

            let dec_path: PathEx = path_to_path_ex_name(path, node.name);
            let sym_t_entry: SymbolTableEntry;
            sym_t_entry.path = dec_path;

            if node.var_comp_type == VarType::Constant{
                sym_t_entry.tag = SymbolTableTag::Constant;
                if !node.has_value{
                    process::throw("constant must have value");
                }
            }else if node.var_comp_type == VarType::Static{
                sym_t_entry.tag = SymbolTableTag::Static;
            }else if node.var_comp_type == VarType::Stack{
                process::throw("let is not allowed in top scope");
            }
            sym_t_entry.data_index = 4294967295 as u32;
            insert_symbol_into_table(symbol_table, sym_t_entry);
            index = index + 1;
            continue;
        }

        for i in 0 as u32 .. 3{
            console::println_i64(statement_vector.array[index + i].statement_type as i64);
            console::write("@@@@\n\r", 6);
        }

        process::throw("--UNEXPECTED STATEMENT--");
    }
}
fn compile_internal_sym_table(path: &Path, statement_vector: &vector::Vec<Stmt>, symbol_table: &SymbolTable, stdout: &string::String) {
    let index: u32 = 0; 
    let len: u32 = statement_vector.length;
    const symbol_vector: &vector::Vec<string::String> = symbol_table.symbol_vector;
    const sym_array: &string::String = symbol_vector.array;
    let loc: PathEx = path_to_path_ex(path);
    while index < len {
        let statement: &Stmt = &statement_vector.array[index];
        if statement.statement_type == StatementType::CompilerHint{
            process::throw("INCLUDE ALREADY HANDLED, OTHERS ARE NOT IMPLEMENTED");
            const node: &HintNode = statement.node_ptr as &HintNode;
            string::append(stdout, '#');
            string::append_str(stdout, sym_array[node.name].data, sym_array[node.name].len);
            string::append(stdout, '(');
            for x in 0 as u32 .. node.expressions.length{
                const expr: &Expression = &node.expressions.array[x];
                debug_dump_expression(expr, symbol_vector, stdout);
                if x != node.expressions.length - 1{
                    string::append_str(stdout, ", ", 2);
                }
            }
            string::append(stdout, ')');
            string::append(stdout, '\n');
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Attribute{
            // Skip. handled below.
            index = index + 1;
            continue;
            const node: &HintNode = statement.node_ptr as &HintNode;
            string::append_str(stdout, "#[", 2);
            string::append_str(stdout, sym_array[node.name].data, sym_array[node.name].len);
            string::append(stdout, '(');
            for x in 0 as u32 .. node.expressions.length{
                const expr: &Expression = &node.expressions.array[x];
                debug_dump_expression(expr, symbol_vector, stdout);
                if x != node.expressions.length - 1{
                    string::append_str(stdout, ", ", 2);
                }
            }
            string::append(stdout, ')');
            string::append_str(stdout, "]\n", 2);
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Function{
            const node: &FnNode = statement.node_ptr as &FnNode;
            let func_path: PathEx = path_to_path_ex_name(path, node.name);
            const sym_t_entry: &SymbolTableEntry = get_symbol_from_table(symbol_table, func_path);

            let llvm_signature: string::String = string::empty();
            append_path_ex(&sym_t_entry.path, symbol_table.symbol_vector.array, &llvm_signature);

            if node.generic.length != 0{
                let sym_entry: GenericFunctionSymbolTableEntry = GenericFunctionSymbolTableEntry::{
                    flags: node.flags,
                    arguments: vector::new::<GenericStructDefinedField>(),
                    return_type: comp_type::void(),
                    is_generic_return: false,
                    body: node.nodes,
                    implementations: vector::new::<Implementation>(),
                    llvm_signature_base: llvm_signature,
                    generic_tokens: vector::new::<u16>(),
                };
                if node.has_value{
                    sym_entry.is_generic_return = comp_type::get_compiler_type_generic(&node.return_type, path, symbol_table, &node.generic, &sym_entry.return_type);
                }
                for x in 0 as u32 .. node.generic.length{
                    vector::push::<u16>(&sym_entry.generic_tokens, node.generic.array[x]);
                }
                for x in 0 as u32 .. node.arguments.length{
                    const arg: &Argument = &node.arguments.array[x];
                    let sdf: GenericStructDefinedField;
                    sdf.name = arg.name;
                    sdf.is_generic = comp_type::get_compiler_type_generic(&arg.type, path, symbol_table, &node.generic, &sdf.type);
                    vector::push::<GenericStructDefinedField>(&sym_entry.arguments, sdf);
                }
                vector::push::<GenericFunctionSymbolTableEntry>(&symbol_table.generic_functions, sym_entry);
                sym_t_entry.data_index = symbol_table.generic_functions.length - 1;
                sym_t_entry.tag = sym_t_entry.tag | SymbolTableTag::Solved;
                index = index + 1;
                continue;
            }

            let sym_entry: FunctionSymbolTableEntry = FunctionSymbolTableEntry::{
                flags: node.flags,
                arguments: vector::new::<StructDefinedField>(),
                return_type: comp_type::void(),
                body: node.nodes,
                llvm_signature: llvm_signature
            };
            if node.has_value{
                sym_entry.return_type = comp_type::get_compiler_type(&node.return_type, path, symbol_table);
            }
            for x in 0 as u32 .. node.arguments.length{
                const arg: &Argument = &node.arguments.array[x];
                let sf: StructDefinedField;
                sf.name = arg.name;
                sf.type = comp_type::get_compiler_type(&arg.type, path, symbol_table);
                vector::push::<StructDefinedField>(&sym_entry.arguments, sf);
            }
            vector::push::<FunctionSymbolTableEntry>(&symbol_table.functions, sym_entry);
            sym_t_entry.data_index = symbol_table.functions.length - 1;

            sym_t_entry.tag = sym_t_entry.tag | SymbolTableTag::Solved;
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Struct{ // Done
            const node: &StructNode = statement.node_ptr as &StructNode;

            let struct_path: PathEx = path_to_path_ex_name(path, node.name);
            const sym_t_entry: &SymbolTableEntry = get_symbol_from_table(symbol_table, struct_path);
            if node.generic.length != 0{
                const sym_entry: &GenericStructSymbolTableEntry = &symbol_table.generic_structs.array[sym_t_entry.data_index];
                for x in 0 as u32 .. node.fields.length{
                    const arg: &Argument = &node.fields.array[x];
                    let sdf: GenericStructDefinedField;
                    sdf.name = arg.name;
                    sdf.is_generic = comp_type::get_compiler_type_generic(&arg.type, path, symbol_table, &node.generic, &sdf.type);
                    vector::push::<GenericStructDefinedField>(&sym_entry.fields, sdf);
                }
                sym_t_entry.tag = sym_t_entry.tag | SymbolTableTag::Solved;
                index = index + 1;
                continue;
            }
            const sym_entry: &StructSymbolTableEntry = &symbol_table.structs.array[sym_t_entry.data_index];
            for x in 0 as u32 .. node.fields.length{
                const node: &Argument = &node.fields.array[x];
                let sdf: StructDefinedField;
                sdf.name = node.name;
                sdf.type = comp_type::get_compiler_type(&node.type, path, symbol_table);
                vector::push::<StructDefinedField>(&sym_entry.fields, sdf);
            }
            sym_t_entry.tag = sym_t_entry.tag | SymbolTableTag::Solved;
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Enum{
            const node: &EnumNode = statement.node_ptr as &EnumNode;
            
            let dec_path: PathEx = path_to_path_ex_name(path, node.name);
            const sym_t_entry: &SymbolTableEntry = get_symbol_from_table(symbol_table, dec_path);
            let sym_entry: EnumSymbolTableEntry = EnumSymbolTableEntry::{
                flags: node.flags,
                representative_type: comp_type::def_integer(),
                fields: vector::new::<EnumDefinedField>(),
            };
            if node.has_value {
                sym_entry.representative_type = comp_type::get_compiler_type(&node.enum_type, path, symbol_table);
            }
            let indexer: u64 = 0;
            for x in 0 as u32 .. node.fields.length{
                const node: &EnumField = &node.fields.array[x];
                let edf: EnumDefinedField;
                edf.name = node.name;
                if node.has_value {
                    let rval: rvalue::Rvalue = expression_compiler::constant_expression_evaluation(&node.value, symbol_table, &loc);
                    if rval.kind != rvalue::RvalueKind::Integer{
                        process::throw("TODO");
                    }
                    indexer = bitcast::<u64>(rval.data);
                }
                edf.value = indexer;
                indexer = indexer + 1;
                vector::push::<EnumDefinedField>(&sym_entry.fields, edf);
            }
            sym_t_entry.tag = sym_t_entry.tag | SymbolTableTag::Solved;
            vector::push::<EnumSymbolTableEntry>(&symbol_table.enums, sym_entry);
            sym_t_entry.data_index = symbol_table.enums.length - 1;
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::Namespace{
            const node: &NamespaceNode = statement.node_ptr as &NamespaceNode;
            let path: Path = *path;
            if path.len == 8{
                process::throw("Nested namespaces too deep");
            }
            path.data[path.len] = node.name;
            path.len = path.len + 1;
            compile_internal_sym_table(&path, &node.nodes, symbol_table, stdout);
            index = index + 1;
            continue;
        } else if statement.statement_type == StatementType::VarDec{
            const node: &DeclarationNode = statement.node_ptr as &DeclarationNode;

            let dec_path: PathEx = path_to_path_ex_name(path, node.name);
            const sym_t_entry: &SymbolTableEntry = get_symbol_from_table(symbol_table, dec_path);
            if sym_t_entry == null{
                process::throw("compile_internal_sym_table_prefill fucked up");
            }
            if node.var_comp_type == VarType::Constant{
                let sym_entry: ConstantSymbolTableEntry = ConstantSymbolTableEntry::{
                    type: comp_type::get_compiler_type(&node.var_type, path, symbol_table),
                    defined_value: expression_compiler::constant_expression_evaluation(&node.expression, symbol_table, &loc),
                };
                sym_t_entry.tag = sym_t_entry.tag | SymbolTableTag::Solved;
                vector::push::<ConstantSymbolTableEntry>(&symbol_table.constants, sym_entry);
                sym_t_entry.data_index = symbol_table.constants.length - 1;
            } else if node.var_comp_type == VarType::Static{
                let sym_entry: StaticSymbolTableEntry = StaticSymbolTableEntry::{
                    has_initialized_value: false,
                    initialized_value: rvalue::void(),
                    type: comp_type::get_compiler_type(&node.var_type, path, symbol_table)
                };
                if node.has_value {
                    sym_entry.has_initialized_value = true;
                    sym_entry.initialized_value = expression_compiler::constant_expression_evaluation(&node.expression, symbol_table, &loc);
                }
                sym_t_entry.tag = sym_t_entry.tag | SymbolTableTag::Solved;
                vector::push::<StaticSymbolTableEntry>(&symbol_table.statics, sym_entry);
                sym_t_entry.data_index = symbol_table.statics.length - 1;
            }
            index = index + 1;
            continue;
        }

        for i in 0 as u32 .. 3{
            console::println_i64(statement_vector.array[index + i].statement_type as i64);
            console::write("@@@@\n\r", 6);
        }

        process::throw("--UNEXPECTED STATEMENT--");
    }
}
namespace compile_internals{
    fn sym_table_push_external(symbol_table: &SymbolTable, stdout: &string::String) {
        const len: u32 = symbol_table.symbols.length;
        const ptr: &SymbolTableEntry = symbol_table.symbols.array;
        const sym_array: &string::String = symbol_table.symbol_vector.array;
        for i in 0 as u32 .. len{
            if (ptr[i].tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::Function{
                const node: &FunctionSymbolTableEntry = &symbol_table.functions.array[ptr[i].data_index];
                if (node.flags & Flags::External) == 0{
                    continue;
                }
                let loc: PathEx = ptr[i].path;
                loc.len = loc.len - 1;

                string::append_str(stdout, "declare dllimport ", 18);
                comp_type::compiler_type_push(&node.return_type, &loc, symbol_table, stdout);
                string::append_str(stdout, "\t@", 2);
                string::append_str(stdout, node.llvm_signature.data, node.llvm_signature.len);
                string::append_str(stdout, "(", 1);            
                for x in 0 as u32 .. node.arguments.length{
                    const arg: &StructDefinedField = &node.arguments.array[x];
                    comp_type::compiler_type_push(&arg.type, &loc, symbol_table, stdout);
                    string::append_str(stdout, " %a", 3);
                    string::append_u64(stdout, x as u64);
                    if x != node.arguments.length - 1{
                        string::append_str(stdout, ", ", 2);
                    }
                }
                string::append_str(stdout, ")", 1);
                string::append_str(stdout, "\n", 1);
            }
        }
    }
    fn sym_table_push_static(symbol_table: &SymbolTable, stdout: &string::String) {
        const len: u32 = symbol_table.symbols.length;
        const ptr: &SymbolTableEntry = symbol_table.symbols.array;
        const sym_array: &string::String = symbol_table.symbol_vector.array;
        for i in 0 as u32 .. len{
            if (ptr[i].tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::Static{
                const node: &StaticSymbolTableEntry = &symbol_table.statics.array[ptr[i].data_index];
                let loc: PathEx = ptr[i].path;
                loc.len = loc.len - 1;

                string::append_str(stdout, "@", 1);
                append_path_ex(&ptr[i].path, sym_array, stdout);
                string::append_str(stdout, " = internal global ", 19);
                comp_type::compiler_type_push(&node.type, &loc, symbol_table, stdout);
                string::append_str(stdout, " zeroinitializer\n", 17);
            }
        }
    }
    fn sym_table_push_structs(symbol_table: &SymbolTable, stdout: &string::String) {
        const len: u32 = symbol_table.symbols.length;
        const ptr: &SymbolTableEntry = symbol_table.symbols.array;
        const sym_array: &string::String = symbol_table.symbol_vector.array;
        for i in 0 as u32 .. len{
            if (ptr[i].tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::Struct{
                const node: &StructSymbolTableEntry = &symbol_table.structs.array[ptr[i].data_index];
                string::append(stdout, '%');
                string::append_str(stdout, node.llvm_signature.data, node.llvm_signature.len);
                string::append_str(stdout, " = type ", 8);
                string::append_str(stdout, "{ ", 2);
                for x in 0 as u32 .. node.fields.length{
                    comp_type::compiler_type_push(&node.fields.array[x].type, &ptr[i].path, symbol_table, stdout);
                    if x != node.fields.length - 1{
                        string::append_str(stdout, ", ", 2);
                    }
                }
                string::append_str(stdout, " }\n", 3);
            }
        }
    }
    fn sym_table_push_structs_generic(symbol_table: &SymbolTable, stdout: &string::String) {
        const len: u32 = symbol_table.symbols.length;
        const ptr: &SymbolTableEntry = symbol_table.symbols.array;
        const sym_array: &string::String = symbol_table.symbol_vector.array;
        for i in 0 as u32 .. len{
            if (ptr[i].tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::GenericStruct{
                const node: &GenericStructSymbolTableEntry = &symbol_table.generic_structs.array[ptr[i].data_index];
                const ilen: u32 = node.implementations.length;
                const iarr: &Implementation = node.implementations.array;
                if node.implementations.length == 0{
                    continue;
                }
                for j in 0 as u32 .. ilen{
                    const current_impl: &Implementation = &iarr[j];
                    string::append(stdout, '%');
                    string::append_str(stdout, current_impl.llvm_signature.data, current_impl.llvm_signature.len);
                    string::append_str(stdout, " = type { ", 10);
                    for x in 0 as u32 .. node.fields.length{
                        if !node.fields.array[x].is_generic{
                            comp_type::compiler_type_push(&node.fields.array[x].type, &ptr[i].path, symbol_table, stdout);
                        }else{
                            string::append_str(stdout, "ptr", 3);
                        }
                        
                        if x != node.fields.length - 1{
                            string::append_str(stdout, ", ", 2);
                        }
                    }
                    string::append_str(stdout, " }\n", 3);
                }
                //string::append(&q.llvm_signature, '.');
                //let p: PathEx = path_to_path_ex(g_path);
                //compiler_type_push(&c.array[0], &p, table, &q.llvm_signature);

            }
        }
    }
    fn sym_table_push_functions(symbol_table: &SymbolTable, stdout: &string::String) {
        const len: u32 = symbol_table.symbols.length;
        const ptr: &SymbolTableEntry = symbol_table.symbols.array;
        const sym_array: &string::String = symbol_table.symbol_vector.array;
        for i in 0 as u32 .. len{
            if (ptr[i].tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::Function{
                const node: &FunctionSymbolTableEntry = &symbol_table.functions.array[ptr[i].data_index];
                if (node.flags & Flags::External) != 0{
                    continue;
                }
                let loc: PathEx = ptr[i].path;
                loc.len = loc.len - 1;

                string::append_str(stdout, "define ", 7);
                comp_type::compiler_type_push(&node.return_type, &ptr[i].path, symbol_table, stdout);
                
                string::append_str(stdout, " @", 2);
                string::append_str(stdout, node.llvm_signature.data, node.llvm_signature.len);
                string::append(stdout, '(');
                for x in 0 as u32 .. node.arguments.length{
                    const arg: &StructDefinedField = &node.arguments.array[x];
                    comp_type::compiler_type_push(&arg.type, &loc, symbol_table, stdout);
                    string::append_str(stdout, " %a", 3);
                    string::append_u64(stdout, x as u64);
                    if x != node.arguments.length - 1{
                        string::append_str(stdout, ", ", 2);
                    }
                }
                string::append(stdout, ')');
                //string::append_str(stdout, node.name.data, node.llvm_signature.len);
                string::append_str(stdout, "{\n", 2);
                let llvm_ir_vec: vector::Vec<output::LLVMInstruction> = vector::new::<output::LLVMInstruction>();
                let scope: scope::Scope = scope::new();
                scope.temp_counter = node.arguments.length;
                compile_body(&node.body, i, 0, &scope, symbol_table, &llvm_ir_vec);
                let vd: comp_type::CompilerType = comp_type::void();
                if comp_type::equal(&node.return_type, &vd){
                    let rv: bool = false;
                    if node.body.length > 0{
                        const statement: &Stmt = &node.body.array[node.body.length - 1];
                        if statement.statement_type == StatementType::Return{
                            rv = true;
                        }
                    }
                    if !rv{
                        vector::push::<output::LLVMInstruction>(&llvm_ir_vec, output::ret_void());
                    }
                }

                output::push_instructions(&llvm_ir_vec, symbol_table, stdout);
                string::append_str(stdout, "}\n", 2);
            }
        }
    }
    fn sym_table_push_constant_data(symbol_table: &SymbolTable, stdout: &string::String) {
        const len: u32 = symbol_table.constant_strings_used.length;
        const ptr: &u32 = symbol_table.constant_strings_used.array;
        const sym_array: &string::String = symbol_table.symbol_vector.array;
        for i in 0 as u32 .. len{
            string::append_str(stdout, "@.d", 3);
            string::append_u64(stdout, i as u64);
            string::append_str(stdout, " = constant [", 13);
            string::append_u64(stdout, sym_array[ptr[i]].len as u64);
            string::append_str(stdout, " x i8] c\"", 9);
            string::append_str(stdout, sym_array[ptr[i]].data, sym_array[ptr[i]].len);
            string::append_str(stdout, "\"\n", 2);
        }
    }
    fn compile_body(statement_vector: &vector::Vec<Stmt>, function_index: u32, implementation_index: u32, scope: &scope::Scope, symbol_table: &SymbolTable, output: &vector::Vec<output::LLVMInstruction>) {
        const p: &SymbolTableEntry = &symbol_table.symbols.array[function_index];
        let f_path: PathEx = p.path;
        let env_path: Path;
        env_path.len = f_path.len - 1;
        for i in 0 as u8 .. env_path.len{
            env_path.data[i] = f_path.data[i];
        }
        let loc: PathEx = f_path;
        loc.len = loc.len - 1;
        const func: &FunctionSymbolTableEntry = &symbol_table.functions.array[p.data_index];
        let index: u32 = 0;
        const len: u32 = statement_vector.length;
        while index < len {
            const statement: &Stmt = &statement_vector.array[index];
            if statement.statement_type == StatementType::VarDec{
                const node: &DeclarationNode = statement.node_ptr as &DeclarationNode;
                const v_type: &Type = &node.var_type;
                const vc_type: comp_type::CompilerType = comp_type::get_compiler_type(v_type, &env_path, symbol_table);
                let var: scope::Variable;
                var.name = node.name;
                var.type = vc_type;
                if node.var_comp_type == VarType::Static{
                    var.is_static = true;   
                }else if node.var_comp_type == VarType::Constant{
                    var.is_constant = true;
                }
                const var_id: u32 = scope.temp_counter; scope.temp_counter = scope.temp_counter + 1;
                var.register = var_id;
                
                const str: &string::String = mem::malloc(size_of::<string::String>()) as &string::String;
                *str = string::empty(); 
                comp_type::compiler_type_push(&var.type, &loc, symbol_table, str);
                vector::push::<output::LLVMInstruction>(output, output::alloc(var_id, str));
                /////////////////
                if node.has_value {
                    let cv: expression_compiler::CompiledValue = expression_compiler::compile(&node.expression, expression_compiler::expect_compiler_type(&var.type), scope, symbol_table, output, &loc);
                    let val_reg: u32 = expression_compiler::cv_to_register(&cv, scope, symbol_table, output, &loc);
                    vector::push::<output::LLVMInstruction>(output, output::store(var_id, str, val_reg));
                }
                scope::add_to_scope(scope, var);
                index = index + 1;
                continue;
            }
            if statement.statement_type == StatementType::Return{
                const statement: &ReturnNode = bitcast::<&ReturnNode>(statement.node_ptr);
                if !statement.has_value{
                    vector::push::<output::LLVMInstruction>(output, output::ret_void());
                    break; // Skip dead code
                }
                const str: &string::String = mem::malloc(size_of::<string::String>()) as &string::String;
                *str = string::empty(); 
                comp_type::compiler_type_push(&func.return_type, &loc, symbol_table, str);

                let check_val: expression_compiler::CompiledValue = expression_compiler::compile(&statement.expression, expression_compiler::expect_anything(), scope, symbol_table, output, &loc);
                const check_val_reg: u32 = expression_compiler::cv_to_register(&check_val, scope, symbol_table, output, &loc);
                vector::push::<output::LLVMInstruction>(output, output::return_op(str, check_val_reg));
                break; // Skip dead code
            }
            if statement.statement_type == StatementType::Expression{
                const node: &ExpressionNode = statement.node_ptr as &ExpressionNode;
                let check_val: expression_compiler::CompiledValue = expression_compiler::compile(&node.expression, expression_compiler::expect_nothing(), scope, symbol_table, output, &loc);
                index = index + 1;
                continue;
            }
            if statement.statement_type == StatementType::Continue{
                if scope.current_logic_id == -1{
                    process::throw("Continue outside of loop");
                }
                vector::push::<output::LLVMInstruction>(output, output::jump_to(scope.current_logic_id as u32));
                break; // Skip dead code
            }
            if statement.statement_type == StatementType::Break{
                if scope.current_logic_id == -1{
                    process::throw("Break outside of loop");
                }
                vector::push::<output::LLVMInstruction>(output, output::jump_to((scope.current_logic_id + 1) as u32));
                break; // Skip dead code
            }
            if statement.statement_type == StatementType::Loop{
                const statement: &LoopStmt = bitcast::<&LoopStmt>(statement.node_ptr);
                const base_id: i32 = scope.current_logic_id as i32;
                const loop_entry: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;
                const loop_exit: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;
                scope.current_logic_id = loop_entry as i32;

                // Explicit fallthrough required by LLVM IR
                vector::push::<output::LLVMInstruction>(output, output::jump_to(loop_entry));
                vector::push::<output::LLVMInstruction>(output, output::label(loop_entry));

                scope::enter_scope(scope);
                compile_body(&statement.body, function_index, implementation_index, scope, symbol_table, output);
                scope::exit_scope(scope);

                vector::push::<output::LLVMInstruction>(output, output::jump_to(loop_entry));
                // Do not emit `jump_to(loop_exit)` here, double terminators are illegal.
                vector::push::<output::LLVMInstruction>(output, output::label(loop_exit));
                scope.current_logic_id = base_id;

                index = index + 1;
                continue;
            }
            if statement.statement_type == StatementType::WhileLoop{
                const statement: &WhileStmt = bitcast::<&WhileStmt>(statement.node_ptr);
                
                const base_id: i32 = scope.current_logic_id as i32;
                const loop_entry: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;
                const loop_exit: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;
                const loop_cond: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;

                scope.current_logic_id = loop_entry as i32;

                // Explicit fallthrough
                vector::push::<output::LLVMInstruction>(output, output::jump_to(loop_entry));
                vector::push::<output::LLVMInstruction>(output, output::label(loop_entry));

                let check_val: expression_compiler::CompiledValue = expression_compiler::compile(&statement.cond, expression_compiler::expect_anything(), scope, symbol_table, output, &loc);
                const check_val_reg: u32 = expression_compiler::cv_to_register(&check_val, scope, symbol_table, output, &loc);
                vector::push::<output::LLVMInstruction>(output, output::branch(check_val_reg, loop_cond, loop_exit));
                vector::push::<output::LLVMInstruction>(output, output::label(loop_cond));

                scope::enter_scope(scope);
                compile_body(&statement.body, function_index, implementation_index, scope, symbol_table, output);
                scope::exit_scope(scope);

                vector::push::<output::LLVMInstruction>(output, output::jump_to(loop_entry));
                // Again, strip illegal double terminator to `loop_exit`
                vector::push::<output::LLVMInstruction>(output, output::label(loop_exit));
                scope.current_logic_id = base_id;

                index = index + 1;
                continue;
            }
            if statement.statement_type == StatementType::DoWhileLoop{
                const statement: &DoWhileStmt = bitcast::<&DoWhileStmt>(statement.node_ptr);
                
                const base_id: i32 = scope.current_logic_id as i32;
                const loop_entry: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;
                const loop_exit: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;
                const loop_cond: u32 = scope.logic_counter; scope.logic_counter = scope.logic_counter + 1;

                scope.current_logic_id = loop_entry as i32;

                // Provide explicit branch to break sequence and prevent stacking unbranched labels
                vector::push::<output::LLVMInstruction>(output, output::jump_to(loop_entry));
                vector::push::<output::LLVMInstruction>(output, output::label(loop_entry));
                vector::push::<output::LLVMInstruction>(output, output::jump_to(loop_cond));
                vector::push::<output::LLVMInstruction>(output, output::label(loop_cond));

                scope::enter_scope(scope);
                compile_body(&statement.body, function_index, implementation_index, scope, symbol_table, output);
                scope::exit_scope(scope);

                let check_val: expression_compiler::CompiledValue = expression_compiler::compile(&statement.cond, expression_compiler::expect_anything(), scope, symbol_table, output, &loc);
                const check_val_reg: u32 = expression_compiler::cv_to_register(&check_val, scope, symbol_table, output, &loc);
                vector::push::<output::LLVMInstruction>(output, output::branch(check_val_reg, loop_cond, loop_exit));

                // Terminations are implicitly done via the branch above so skip the illegal end block jumps
                vector::push::<output::LLVMInstruction>(output, output::label(loop_exit));
                scope.current_logic_id = base_id;

                index = index + 1;
                continue;
            }
            if statement.statement_type == StatementType::If{
                const statement: &IfStmt = bitcast::<&IfStmt>(statement.node_ptr);
                const base_id: i32 = scope.current_logic_id as i32;
                
                let check_val: expression_compiler::CompiledValue = expression_compiler::compile(&statement.cond, expression_compiler::expect_anything(), scope, symbol_table, output, &loc);
                const check_val_reg: u32 = expression_compiler::cv_to_register(&check_val, scope, symbol_table, output, &loc);

                const entry_lbl: u32 = scope.logic_counter; 
                const else_lbl: u32 = scope.logic_counter + 1;
                let end_lbl: u32 = scope.logic_counter + 2;
                scope.logic_counter = scope.logic_counter + 3;
                if statement.else_body.length == 0{
                    end_lbl = else_lbl;
                    scope.logic_counter = scope.logic_counter - 1;
                }
                vector::push::<output::LLVMInstruction>(output, output::branch(check_val_reg, entry_lbl, else_lbl));

                vector::push::<output::LLVMInstruction>(output, output::label(entry_lbl));
                compile_body(&statement.then_body, function_index, implementation_index, scope, symbol_table, output);
                vector::push::<output::LLVMInstruction>(output, output::jump_to(end_lbl));
                if statement.else_body.length != 0{
                    vector::push::<output::LLVMInstruction>(output, output::label(else_lbl));
                    compile_body(&statement.else_body, function_index, implementation_index, scope, symbol_table, output);
                    vector::push::<output::LLVMInstruction>(output, output::jump_to(end_lbl));
                }
                vector::push::<output::LLVMInstruction>(output, output::label(end_lbl));
                scope.current_logic_id = base_id;
                index = index + 1;
                continue;
            }
            if statement.statement_type == StatementType::ForLoop{
                const node: &ForStmt = statement.node_ptr as &ForStmt;
                index = index + 1;
                continue;
            }
            vector::push::<output::LLVMInstruction>(output, output::unreachable());
            break; // Skip dead code
        }
    }
}
fn compile(statement_vector: &vector::Vec<Stmt>, symbol_vector: &vector::Vec<string::String>, stdout: &string::String) {
    layout::STATIC_layout();
    primitives::STATIC_primitives();

    let sym_table: SymbolTable;
    sym_table.symbols = vector::new::<SymbolTableEntry>();
    sym_table.symbol_vector = symbol_vector;

    sym_table.constant_strings_used = vector::new::<u32>();
    sym_table.functions = vector::new::<FunctionSymbolTableEntry>();
    sym_table.generic_functions = vector::new::<GenericFunctionSymbolTableEntry>();
    sym_table.structs = vector::new::<StructSymbolTableEntry>();
    sym_table.generic_structs = vector::new::<GenericStructSymbolTableEntry>();
    sym_table.enums = vector::new::<EnumSymbolTableEntry>();
    sym_table.constants = vector::new::<ConstantSymbolTableEntry>();
    sym_table.statics = vector::new::<StaticSymbolTableEntry>();
    
    let path: Path; path.len = 0;
    compile_internal_sym_table_prefill(&path, statement_vector, &sym_table, stdout);
    compile_internal_sym_table(&path, statement_vector, &sym_table, stdout);

    compile_internals::sym_table_push_structs(&sym_table, stdout);

    let function_out: string::String = string::empty();
    compile_internals::sym_table_push_functions(&sym_table, &function_out);
    compile_internals::sym_table_push_structs_generic(&sym_table, stdout);
    compile_internals::sym_table_push_external(&sym_table, stdout);
    compile_internals::sym_table_push_static(&sym_table, stdout);
    compile_internals::sym_table_push_constant_data(&sym_table, stdout);

    string::append_str(stdout, function_out.data, function_out.len);
    string::free(&function_out);
}
namespace expression_compiler{
    pub enum ExpectedKind { Nothing, Type, Anything }
    pub struct Expected{
        kind: ExpectedKind,
        compiler_type: &comp_type::CompilerType
    }
    pub fn expect_nothing() : Expected {let x: Expected; x.kind = ExpectedKind::Nothing; return x;}
    pub fn expect_anything() : Expected {let x: Expected; x.kind = ExpectedKind::Anything; return x;}
    pub fn expect_compiler_type(ct: &comp_type::CompilerType) : Expected {let x: Expected; x.kind = ExpectedKind::Type; x.compiler_type = ct; return x;}
    
    pub enum CompiledValueKind{ Value, NoReturn }
    pub struct CompiledValue{
        kind: CompiledValueKind,
        type: comp_type::CompilerType,
        rvalue: rvalue::Rvalue,
    }
    pub fn cv_register(register: u32, type: comp_type::CompilerType) : CompiledValue {let x: CompiledValue = CompiledValue::{kind: CompiledValueKind::Value, type: type, rvalue: rvalue::register(register)}; return x;}
    pub fn cv_integer(integer: u64) : CompiledValue {let x: CompiledValue = CompiledValue::{kind: CompiledValueKind::Value, type: comp_type::def_integer(), rvalue: rvalue::integer(integer)}; return x;}
    pub fn cv_decimal(decimal: f64) : CompiledValue {let x: CompiledValue = CompiledValue::{kind: CompiledValueKind::Value, type: comp_type::def_decimal(), rvalue: rvalue::decimal(decimal)}; return x;}
    pub fn cv_bool(val: bool) : CompiledValue {let x: CompiledValue = CompiledValue::{kind: CompiledValueKind::Value, type: comp_type::def_bool(), rvalue: rvalue::boolean(val)}; return x;}
    pub fn cv_null_untyped() : CompiledValue {let x: CompiledValue = CompiledValue::{kind: CompiledValueKind::Value, type: comp_type::def_pointer(), rvalue: rvalue::nil()}; return x;}
    
    pub fn cv_to_register(cv: &CompiledValue, scope: &scope::Scope, symbol_table: &SymbolTable, output: &vector::Vec<output::LLVMInstruction>, env_path: &PathEx) : u32 {
        if cv.kind != CompiledValueKind::Value{
            process::throw("cv_to_register");
        }
        if cv.rvalue.kind == rvalue::RvalueKind::Register{
            return cv.rvalue.data as u32;
        } else if cv.rvalue.kind == rvalue::RvalueKind::Integer || cv.rvalue.kind == rvalue::RvalueKind::Boolean{
            const temp_id: u32 = scope.temp_counter; scope.temp_counter = scope.temp_counter + 1;
            const str: &string::String = mem::malloc(size_of::<string::String>()) as &string::String;
            *str = string::empty(); 
            comp_type::compiler_type_push(&cv.type, env_path, symbol_table, str);
            vector::push::<output::LLVMInstruction>(output, output::load_integer(temp_id, str, cv.rvalue.data as u64));
            return temp_id;
        } else if cv.rvalue.kind == rvalue::RvalueKind::Null{
            const temp_id: u32 = scope.temp_counter; scope.temp_counter = scope.temp_counter + 1;
            const str: &string::String = mem::malloc(size_of::<string::String>()) as &string::String;
            *str = string::empty(); 
            comp_type::compiler_type_push(&cv.type, env_path, symbol_table, str);
            vector::push::<output::LLVMInstruction>(output, output::load_null(temp_id, str));
            return temp_id;
        }
        process::throw("cv_to_register");
        return 555;
    }
    pub fn lv_to_register(lv: &lvalue::Lvalue, scope: &scope::Scope, symbol_table: &SymbolTable, output: &vector::Vec<output::LLVMInstruction>, env_path: &PathEx) : u32 {
        if lv.kind == lvalue::LvalueKind::Variable{
            return lv.data as u32;
        }
        return 4567456;
    }
    pub fn compile(expression: &Expression, expect: Expected, scope: &scope::Scope, symbol_table: &SymbolTable, output: &vector::Vec<output::LLVMInstruction>, env_path: &PathEx) : expression_compiler::CompiledValue{
        return compile_rval(expression, expect, scope, symbol_table, output, env_path);
    }
    pub fn compile_rval(expression: &Expression, expect: Expected, scope: &scope::Scope, symbol_table: &SymbolTable, output: &vector::Vec<output::LLVMInstruction>, env_path: &PathEx) : expression_compiler::CompiledValue{
        let expect: Expected = expect;
        if expression.type == ExpressionType::Integer{
            const data: &IntegerExpr = expression.val as &IntegerExpr;
            const str: &string::String = &symbol_table.symbol_vector.array[data.idx];
            const integer: &u64 = str.data as &u64;
            if expect.kind != ExpectedKind::Type {
                return cv_integer(*integer);
            }
            const compiler_type: &comp_type::CompilerType = expect.compiler_type;
            let c: CompiledValue = cv_integer(*integer);
            c.type = comp_type::clone(compiler_type);
            return c;
        }
        if expression.type == ExpressionType::Decimal{
            const data: &DecimalExpr = expression.val as &DecimalExpr;
            const str: &string::String = &symbol_table.symbol_vector.array[data.idx];
            const decimal: &f64 = str.data as &f64;
            if expect.kind != ExpectedKind::Type {
                return cv_decimal(*decimal);
            }
            console::println_i64(*decimal as i64);
            return cv_decimal(*decimal);
        }
        if expression.type == ExpressionType::Boolean{
            const data: &BoolExpr = expression.val as &BoolExpr;
            return cv_bool(data.val);
        }
        if expression.type == ExpressionType::NullPtr{
            if expect.kind == ExpectedKind::Type{
                return cv_null_untyped();
            }else if expect.kind == ExpectedKind::Anything{
                return cv_null_untyped();
            }
        }
        if expression.type == ExpressionType::BinaryOp{
            const data: &BinaryOpExpr = expression.val as &BinaryOpExpr;
            if data.op == BinaryOp::Less || data.op == BinaryOp::Greater || data.op == BinaryOp::LessEqual || data.op == BinaryOp::GreaterEqual
            || data.op == BinaryOp::Equals || data.op == BinaryOp::NotEqual 
            || data.op == BinaryOp::And || data.op == BinaryOp::Or{
                return cv_bool(false);
            }
            if data.op == BinaryOp::Assign{
                return cv_integer(312);
            }

            return cv_integer(312);
        }
        if expression.type == ExpressionType::UnaryOp{
            const data: &UnaryOpExpr = expression.val as &UnaryOpExpr;
            if data.op == UnaryOp::Not{
                return cv_bool(false);
            }
            if data.op == UnaryOp::Pointer{
                let data: lvalue::Lvalue = compile_lval(&data.rval, scope, symbol_table, output, env_path);
                let cv: CompiledValue = cv_register(lv_to_register(&data, scope, symbol_table, output, env_path), data.type);
                cv.type.pointered_deep = cv.type.pointered_deep + 1;
                return cv;
            }
            return cv_integer(55523);
        }
        if expression.type == ExpressionType::Name{
            const data: &NameExpr = expression.val as &NameExpr;
            
        }
        if expression.type == ExpressionType::StaticAccess{
            const data: &StaticAccessExpr = expression.val as &StaticAccessExpr;
            
        }
        if expression.type == ExpressionType::Char{
            const data: &CharExpr = expression.val as &CharExpr;
        }
        if expression.type == ExpressionType::StringConst{
            const data: &StringConstExpr = expression.val as &StringConstExpr;
            let index: i32 = -1;
            for i in 0 as u32 .. symbol_table.constant_strings_used.length{
                if symbol_table.constant_strings_used.array[i] == data.idx as u32{
                    index = i as i32;
                    break;
                }
            }
            if index == -1{
                index = symbol_table.constant_strings_used.length as i32;
                vector::push::<u32>(&symbol_table.constant_strings_used, data.idx as u32);
            }
            const temp_id: u32 = scope.temp_counter; scope.temp_counter = scope.temp_counter + 1;
            vector::push::<output::LLVMInstruction>(output, output::load_bytes(temp_id, index as u32));
            let r: CompiledValue = cv_null_untyped();
            r.rvalue = rvalue::register(temp_id);
            return r;
        }
        if expression.type == ExpressionType::Call{
            const data: &CallExpr = expression.val as &CallExpr;
            let call_site: lvalue::Lvalue = compile_lval(&data.lval, scope, symbol_table, output, env_path);
            if call_site.type.kind != comp_type::CompilerTypeKind::Function{
                process::throw("Tried to call not a function");
            }
            const ptr: &comp_type::FunctionCompilerType = bitcast::<&comp_type::FunctionCompilerType>(call_site.type.data);

            const cs: &string::String = mem::malloc(size_of::<string::String>()) as &string::String;
            *cs = string::empty();
            comp_type::compiler_type_push(&ptr.return_type, env_path, symbol_table, cs);
            string::append(cs, ' ');
            string::append_str(cs, call_site.location.data, call_site.location.len);



            const ag: &string::String = mem::malloc(size_of::<string::String>()) as &string::String;
            *ag = string::empty();
            for i in 0 as u32 .. ptr.arguments.length{
                let x: expression_compiler::CompiledValue = compile_rval(&data.expr.array[i], expect_anything(), scope, symbol_table, output, env_path);
                comp_type::compiler_type_push(&ptr.arguments.array[i], env_path, symbol_table, ag);
                string::append_str(ag, " %t", 3);
                string::append_u64(ag, cv_to_register(&x, scope, symbol_table, output, env_path) as u64);
                if i != ptr.arguments.length - 1{
                    string::append_str(ag, ", ", 2);
                }
            }

            const temp_id: u32 = scope.temp_counter; scope.temp_counter = scope.temp_counter + 1;
            vector::push::<output::LLVMInstruction>(output, output::call(temp_id, cs, ag));
            return cv_register(temp_id, comp_type::clone(&ptr.return_type));
        }
        return cv_integer(1337);
    }
    pub fn compile_lval(expression: &Expression, scope: &scope::Scope, symbol_table: &SymbolTable, output: &vector::Vec<output::LLVMInstruction>, env_path: &PathEx) : lvalue::Lvalue{
        let lval: lvalue::Lvalue;
        if expression.type == ExpressionType::Name{
            const data: &NameExpr = expression.val as &NameExpr;
            const var: &scope::Variable = scope::find_variable(scope, data.idx);
            if var != null{
                lval.kind = lvalue::LvalueKind::Variable;
                lval.data = var.register as u64;
                lval.type = comp_type::clone(&var.type);
                return lval;
            }
            let glob: PathEx = *env_path;
            glob.data[glob.len] = data.idx;
            glob.len = glob.len + 1;
            const ste: &SymbolTableEntry = get_symbol_from_table(symbol_table, glob);
            if (ste.tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::Function{
                lval.kind = lvalue::LvalueKind::Function;
                const node: &FunctionSymbolTableEntry = &symbol_table.functions.array[ste.data_index];
                lval.location = string::empty();
                string::append(&lval.location, '@');
                string::append_str(&lval.location, node.llvm_signature.data, node.llvm_signature.len);
                let x: vector::Vec<comp_type::CompilerType> = vector::new::<comp_type::CompilerType>();
                for i in 0 as u32 .. node.arguments.length{
                    vector::push::<comp_type::CompilerType>(&x, comp_type::clone(&node.arguments.array[i].type));
                }
                lval.type = comp_type::function(comp_type::clone(&node.return_type), x);
                return lval;

            }
        }
        return lval;
    }


    fn constant_expression_evaluation(expr: &Expression, table: &SymbolTable, env_path: &PathEx) : rvalue::Rvalue{
        return rvalue::integer(19999991);
    }
}
namespace scope{
    pub struct Variable{
        name: u16,
        type: comp_type::CompilerType,
        is_argument: bool,
        is_static: bool,
        is_constant: bool,
        register: u32,
    }
    pub struct Scope{
        values: vector::Vec<Variable>,
        scope_index: vector::Vec<u32>,
        temp_counter: u32,
        logic_counter: u32,
        current_logic_id: i32,
    }
    pub fn new() : Scope{
        let scope: Scope = Scope::{ 
            values: vector::new::<Variable>(),
            scope_index: vector::new::<u32>(),
            temp_counter: 0,
            logic_counter: 0,
            current_logic_id: -1,
        };
        return scope;
    }
    pub fn free(scope: &Scope){
        for x in 0 as u32 .. scope.values.length{
            comp_type::free(&scope.values.array[x].type);
        }
        vector::free::<Variable>(&scope.values);
        vector::free::<u32>(&scope.scope_index);
    }
    pub fn add_to_scope(scope: &Scope, val: Variable){
        vector::push::<Variable>(&scope.values, val);
    }
    pub fn enter_scope(scope: &Scope){
        vector::push::<u32>(&scope.scope_index, scope.values.length);
    }
    pub fn exit_scope(scope: &Scope){
        let start_index: u32 = 0;
        if scope.scope_index.length != 0{
            start_index = vector::pop::<u32>(&scope.scope_index);
        }
        while scope.values.length > start_index {
            const var: Variable = vector::pop::<Variable>(&scope.values);
        }
    }
    pub fn last_variable(scope: &Scope) : &Variable{
        const v_len: u32 = scope.values.length;
        if v_len == 0{
            return null;
        }
        return &scope.values.array[v_len - 1];
    }
    pub fn find_variable(scope: &Scope, name: u16) : &Variable{
        const v_len: u32 = scope.values.length;
        let i: u32 = scope.values.length;
        while i > 0{
            i = i - 1;
            const var: &Variable = &scope.values.array[i];
            if var.name == name{
                return var;
            }
        }
        return null;
    }
}
namespace comp_type{
    pub enum CompilerTypeKind: u8{
        ConstantSizeArray,
        Primitive, Struct, Function,
        GenericPlaceholder, GenericImplementation,
    }
    pub struct CompilerType{
        kind: CompilerTypeKind,
        pointered_deep: u8,
        extra: u32,
        data: u64,
    }
    pub struct ConstantArrayCompilerType{
        elements_count: u32,
        inner: CompilerType,
    }
    pub struct FunctionCompilerType{
        return_type: CompilerType,
        arguments: vector::Vec<CompilerType>,
    }
    pub inline fn void() : CompilerType => primitive(primitives::VOID_TYPE);
    pub inline fn def_integer() : CompilerType => primitive(primitives::DEFAULT_INTEGER_TYPE);
    pub inline fn def_bool() : CompilerType => primitive(primitives::DEFAULT_BOOL_TYPE);
    pub inline fn def_decimal() : CompilerType => primitive(primitives::DEFAULT_DECIMAL_TYPE);
    pub inline fn def_pointer() : CompilerType{let x: CompilerType = primitive(primitives::VOID_TYPE); x.pointered_deep = 1; return x;}
    pub inline fn primitive(val: &primitives::PrimitiveTypeInfo) : CompilerType {let x: CompilerType = CompilerType::{kind:CompilerTypeKind::Primitive, pointered_deep: 0, extra:0, data: bitcast::<u64>(val)}; return x;}

    pub inline fn structure(structure_index: i32) : CompilerType {let x: CompilerType = CompilerType::{kind:CompilerTypeKind::Struct, pointered_deep: 0, extra:0, data: structure_index as u64}; return x;}
    pub inline fn placeholder(placeholder_name: u16) : CompilerType {let x: CompilerType = CompilerType::{kind:CompilerTypeKind::GenericPlaceholder, pointered_deep: 0, extra:0, data: placeholder_name as u64}; return x;}
    pub inline fn implementation(structure_index: u32, implementation_index: u32) : CompilerType {let x: CompilerType = CompilerType::{kind:CompilerTypeKind::GenericImplementation, pointered_deep: 0, extra: implementation_index, data: structure_index as u64}; return x;}
    pub inline fn pointer(type: CompilerType, pointer_deep: u8) : CompilerType {
        let x: CompilerType = type;
        x.pointered_deep = x.pointered_deep + pointer_deep;
        return x;
    }
    pub inline fn constant_array(type: CompilerType, elements_count: u32) : CompilerType {
        let x: CompilerType = CompilerType::{kind:CompilerTypeKind::ConstantSizeArray, pointered_deep: 0, extra: 0, data: 0};
        const ptr: &ConstantArrayCompilerType = mem::malloc(size_of::<ConstantArrayCompilerType>()) as &ConstantArrayCompilerType;
        ptr.elements_count = elements_count;
        ptr.inner = type;
        x.data = bitcast::<u64>(ptr);
        return x;
    }
    pub inline fn function(return_type: CompilerType, arguments: vector::Vec<CompilerType>) : CompilerType {
        let x: CompilerType = CompilerType::{kind:CompilerTypeKind::Function, pointered_deep: 0, extra: 0, data: 0};
        const ptr: &FunctionCompilerType = mem::malloc(size_of::<FunctionCompilerType>()) as &FunctionCompilerType;
        ptr.return_type = return_type;
        ptr.arguments = arguments;
        x.data = bitcast::<u64>(ptr);
        return x;
    }
    pub fn get_compiler_type(type: &Type, g_path: &Path, table: &SymbolTable): CompilerType{
        let l_path: Path; l_path.len = 0;
        return get_compiler_type_internal(type, &l_path, g_path, table);
    }
    pub fn get_compiler_type_generic(type: &Type, g_path: &Path, table: &SymbolTable, generics: &vector::Vec<u16>, output: &CompilerType): bool{
        let l_path: Path; l_path.len = 0;
        return get_compiler_type_generic_internal(type, &l_path, g_path, table, generics, output);
    }
    fn get_compiler_type_internal(type: &Type, l_path: &Path, g_path: &Path, table: &SymbolTable): CompilerType{
        if type.type == TypeType::Named{
            const node: &NamedType = type.val as &NamedType;
            const str: &string::String = &table.symbol_vector.array[node.idx];

            const pti: &primitives::PrimitiveTypeInfo = primitives::find_primitive_type(str.data, str.len);
            if pti != null{
                return primitive(pti);
            }
            const p: PathEx = path_to_path_ex_name(l_path, node.idx);
            let ptr: &SymbolTableEntry = get_symbol_from_table(table, p);
            if ptr == null{
                const p: PathEx = path_to_path_ex_name(g_path, node.idx);
                const x: &SymbolTableEntry = get_symbol_from_table(table, p);
                ptr = x;
            }
            if ptr == null{
                return void();
            }
            const index: i32 = ((bitcast::<usize>(ptr) - bitcast::<usize>(table.symbols.array)) / size_of::<SymbolTableEntry>()) as i32;
            return structure(index);

        } else if type.type == TypeType::Pointer{
            const node: &PointerType = type.val as &PointerType;
            const inner: CompilerType = get_compiler_type_internal(&node.inner, l_path, g_path, table);
            return pointer(inner, node.deep);
        } else if type.type == TypeType::NamespaceLink{
            const node: &NamespaceLinkType = type.val as &NamespaceLinkType;
            return get_compiler_type_internal(&node.rval, &node.path, g_path, table);
        } else if type.type == TypeType::Generic{
            const node: &GenericType = type.val as &GenericType;
            const str: &string::String = &table.symbol_vector.array[node.idx];
            const pti: &primitives::PrimitiveTypeInfo = primitives::find_primitive_type(str.data, str.len);
            if pti != null{
                process::throw("TODO GENERIC");
            }
            const p: PathEx = path_to_path_ex_name(l_path, node.idx);
            let ptr: &SymbolTableEntry = get_symbol_from_table(table, p);
            if ptr == null{
                const p: PathEx = path_to_path_ex_name(g_path, node.idx);
                const x: &SymbolTableEntry = get_symbol_from_table(table, p);
                ptr = x;
            }
            if ptr == null{
                process::throw("Name not found");
            }
            const index: u32 = ((bitcast::<usize>(ptr) - bitcast::<usize>(table.symbols.array)) / size_of::<SymbolTableEntry>()) as u32;
            
            let c: vector::Vec<CompilerType> = vector::new::<CompilerType>();
            for i in 0 as u32 .. node.gen_args.length{
                vector::push::<CompilerType>(&c, get_compiler_type(&node.gen_args.array[i], g_path, table));
            }
            const gi: &GenericStructSymbolTableEntry = &table.generic_structs.array[ptr.data_index];
            
            let impl_index: u32 = 65535;
            for i in 0 as u32 .. gi.implementations.length{
                const imp: &vector::Vec<CompilerType> = &gi.implementations.array[i].type;
                let same: bool = true;
                for j in 0 as u32 .. imp.length{
                    if !equal(&imp.array[j], &c.array[j]){
                        same = false;
                        break;
                    }
                }
                if same {
                    impl_index = i;
                    break;
                }
            }
            if impl_index == 65535{
                impl_index = gi.implementations.length;
                let q: Implementation;
                q.type = c;
                q.layout = layout::invalid();
                q.llvm_signature = string::clone(&gi.llvm_signature_base);
                string::append(&q.llvm_signature, '.');
                let p: PathEx = path_to_path_ex(g_path);
                compiler_type_push_internal(&c.array[0], &p, table, &q.llvm_signature, true);
                vector::push::<Implementation>(&gi.implementations, q);
            }else{
                for x in 0 as u32 .. c.length{
                    comp_type::free(&c.array[x]);
                }
                vector::free::<CompilerType>(&c);
            }
            return implementation(index, impl_index);
        } else if type.type == TypeType::ConstantSizeArray{
            const node: &ConstantSizeArrayType = type.val as &ConstantSizeArrayType;
            const inner: CompilerType = get_compiler_type_internal(&node.inner, l_path, g_path, table);
            let loc: PathEx = path_to_path_ex(g_path);
            let ev: rvalue::Rvalue = expression_compiler::constant_expression_evaluation(&node.size, table, &loc);
            if ev.kind != rvalue::RvalueKind::Integer{
                process::throw("TODO, ERROR");
            }
            return constant_array(inner, bitcast::<usize>(ev.data) as u32);
        }
        return void();
    }
    fn get_compiler_type_generic_internal(type: &Type, l_path: &Path, g_path: &Path, table: &SymbolTable, generics: &vector::Vec<u16>, output: &CompilerType): bool{
        if type.type == TypeType::Named{
            const node: &NamedType = type.val as &NamedType;
            const str: &string::String = &table.symbol_vector.array[node.idx];
            
            for i in 0 as u32 .. generics.length{
                if generics.array[i] == node.idx{
                    *output = placeholder(node.idx);
                    return true;
                }
            }

            const pti: &primitives::PrimitiveTypeInfo = primitives::find_primitive_type(str.data, str.len);
            if pti != null{
                *output = primitive(pti);
                return false;
            }
            const p: PathEx = path_to_path_ex_name(l_path, node.idx);
            let ptr: &SymbolTableEntry = get_symbol_from_table(table, p);
            if ptr == null{
                const p: PathEx = path_to_path_ex_name(g_path, node.idx);
                const x: &SymbolTableEntry = get_symbol_from_table(table, p);
                ptr = x;
            }
            if ptr == null{
                *output = void();
                return false;
            }
            const index: i32 = ((bitcast::<usize>(ptr) - bitcast::<usize>(table.symbols.array)) / size_of::<SymbolTableEntry>()) as i32;
            *output = structure(index);
            return false;
        } else if type.type == TypeType::Pointer{
            const node: &PointerType = type.val as &PointerType;
            let inner: CompilerType;
            const gener: bool = get_compiler_type_generic_internal(&node.inner, l_path, g_path, table, generics, &inner);
            *output = pointer(inner, node.deep);
            return gener;
        } else if type.type == TypeType::NamespaceLink{
            const node: &NamespaceLinkType = type.val as &NamespaceLinkType;
            let inner: CompilerType;
            const q: bool = get_compiler_type_generic_internal(&node.rval, &node.path, g_path, table, generics, &inner);
            *output = inner;
            return q;
        }
        *output = void();
        return false;
    }
    pub fn compiler_type_push(type: &CompilerType, path: &PathEx, symbol_table: &SymbolTable, stdout: &string::String){
        compiler_type_push_internal(type, path, symbol_table, stdout, false);
    }
    pub fn compiler_type_push_internal(type: &CompilerType, path: &PathEx, symbol_table: &SymbolTable, stdout: &string::String, inside_generic: bool){
        if type.kind == CompilerTypeKind::Primitive{
            const node: &primitives::PrimitiveTypeInfo = bitcast::<&primitives::PrimitiveTypeInfo>(type.data);
            if type.pointered_deep > 0 && node.layout.size == 0{
                string::append_str(stdout, "ptr", 3);
                return;
            }else{
                string::append_str(stdout, node.llvm_signature, string_utils::c_str_len(node.llvm_signature));
            }
        } else if type.kind == CompilerTypeKind::Struct{
            const struct_index: u32 = type.data as u32;
            const s: &SymbolTableEntry = &symbol_table.symbols.array[struct_index];
            if (s.tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::Struct{
                const c: &StructSymbolTableEntry = &symbol_table.structs.array[s.data_index];
                if !inside_generic{ string::append(stdout, '%'); }
                string::append_str(stdout, c.llvm_signature.data, c.llvm_signature.len);
            } else if (s.tag & SymbolTableTag::OnlyTypesMask) == SymbolTableTag::Enum{
                const e: &EnumSymbolTableEntry = &symbol_table.enums.array[s.data_index];
                compiler_type_push_internal(&e.representative_type, path, symbol_table, stdout, inside_generic);
            }
        } else if type.kind == CompilerTypeKind::GenericImplementation{
            let structure_index: u32 = type.data as u32;
            let implementation_index: u32 = type.extra;

            const s: &SymbolTableEntry = &symbol_table.symbols.array[structure_index];
            const c: &GenericStructSymbolTableEntry = &symbol_table.generic_structs.array[s.data_index];
            const imp: &Implementation = &c.implementations.array[implementation_index];
            
            if !inside_generic{ string::append(stdout, '%'); }
            string::append_str(stdout, imp.llvm_signature.data, imp.llvm_signature.len);
            
        } else if type.kind == CompilerTypeKind::ConstantSizeArray{
            const node: &ConstantArrayCompilerType = bitcast::<&ConstantArrayCompilerType>(type.data);
            string::append(stdout, '[');
            string::append_u64(stdout, node.elements_count as u64);
            string::append_str(stdout, " x ", 3);
            
            compiler_type_push_internal(&node.inner, path, symbol_table, stdout, inside_generic);
            string::append(stdout, ']');
        }
        if type.pointered_deep > 0 {
            for i in 0 as u8 .. type.pointered_deep { string::append(stdout, '*'); }
            //string::append_str(stdout, "ptr", 3);
        }
    }

    pub fn clone(var: &CompilerType) : CompilerType{
        let x: CompilerType = CompilerType::{
            kind: var.kind, 
            pointered_deep: var.pointered_deep, 
            extra: var.extra, 
            data: var.data
        };
        return x;
    }
    pub fn equal(l: &CompilerType, r: &CompilerType): bool{
        if l.kind != r.kind || l.pointered_deep != r.pointered_deep {
            return false;
        }
        if l.kind == CompilerTypeKind::ConstantSizeArray {
            const lp: &ConstantArrayCompilerType = bitcast::<&ConstantArrayCompilerType>(l.data as usize);
            const rp: &ConstantArrayCompilerType = bitcast::<&ConstantArrayCompilerType>(r.data as usize);
            return lp.elements_count == rp.elements_count && equal(&lp.inner, &rp.inner);
        }
        return l.data == r.data && l.extra == r.extra;
    }
    pub fn free(var: &CompilerType){
        if var.kind == CompilerTypeKind::ConstantSizeArray && var.data != 0 {
            const ptr: &ConstantArrayCompilerType = bitcast::<&ConstantArrayCompilerType>(var.data as usize);
            comp_type::free(&ptr.inner);
            
            mem::free(bitcast::<&void>(var.data));
            var.data = 0;
        }else if var.kind == CompilerTypeKind::Function && var.data != 0 {
            const ptr: &FunctionCompilerType = bitcast::<&FunctionCompilerType>(var.data as usize);
            comp_type::free(&ptr.return_type);
            for i in 0 as u32 .. ptr.arguments.length{
                const x: &CompilerType = &ptr.arguments.array[i];
                comp_type::free(x);
            }
            vector::free::<CompilerType>(&ptr.arguments);
            mem::free(bitcast::<&void>(var.data));
            var.data = 0;
        }
    }
}
namespace lvalue{
    pub enum LvalueKind{
        Function,
        Variable,
    }
    pub struct Lvalue{
        kind: LvalueKind,
        location: string::String,
        data: u64,
        type: comp_type::CompilerType,
    }
}
namespace rvalue{
    pub enum RvalueKind{
        Register,
        Integer,Decimal,
        Boolean, Null, Void,
        Nothing,
    }
    pub struct Rvalue{
        kind: RvalueKind,
        data: u64
    }
    pub fn register(val: u32) : Rvalue {let x: Rvalue = Rvalue::{kind:RvalueKind::Register, data: val as u64}; return x;}
    pub fn integer(val: u64) : Rvalue {let x: Rvalue = Rvalue::{kind:RvalueKind::Integer, data: val}; return x;}
    pub fn decimal(val: f64) : Rvalue {let x: Rvalue = Rvalue::{kind:RvalueKind::Decimal, data: bitcast::<u64>(val)}; return x;}
    pub fn boolean(val: bool) : Rvalue {let x: Rvalue = Rvalue::{kind:RvalueKind::Boolean, data: val as u64}; return x;}
    pub fn nil() : Rvalue {let x: Rvalue = Rvalue::{kind:RvalueKind::Null, data:0}; return x;}
    pub fn void() : Rvalue {let x: Rvalue = Rvalue::{kind:RvalueKind::Void, data:0}; return x;}
}
namespace layout{
    pub struct Layout { size: u16, align: u16 }
    pub inline fn invalid():Layout{let x: Layout; x.size = 65535; x.align = 65535; return x;}
    pub inline fn void():   Layout{let x: Layout; x.size = 0; x.align = 1; return x;}
    pub inline fn one():    Layout{let x: Layout; x.size = 1; x.align = 1; return x;}
    pub inline fn two():    Layout{let x: Layout; x.size = 2; x.align = 2; return x;}
    pub inline fn four():   Layout{let x: Layout; x.size = 4; x.align = 4; return x;}
    pub inline fn eight():  Layout{let x: Layout; x.size = 8; x.align = 8; return x;}
    pub inline fn equal(l: &Layout, r: &Layout): bool => l.size == r.size && l.align == r.align;
    pub static POINTER_LAYOUT: Layout;
    pub fn STATIC_layout() { POINTER_LAYOUT = layout::eight(); }
}
namespace primitives{
    pub enum PrimitiveKind {
        Void, Bool,
        SignedInt, UnsignedInt,
        Decimal,
    }
    pub struct PrimitiveTypeInfo{
        name: &i8,
        llvm_signature: &i8,
        layout: layout::Layout,
        kind: PrimitiveKind,
    }
    pub static VOID_TYPE: &PrimitiveTypeInfo;
    pub static DEFAULT_INTEGER_TYPE: &PrimitiveTypeInfo;
    pub static DEFAULT_BOOL_TYPE: &PrimitiveTypeInfo;
    pub static DEFAULT_DECIMAL_TYPE: &PrimitiveTypeInfo;
    pub static PRIMITIVE_TYPES_INFO: [PrimitiveTypeInfo; 15];
    pub fn STATIC_primitives() {
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "void",llvm_signature: "void",
            layout: layout::void(),
            kind: PrimitiveKind::Void
        }; PRIMITIVE_TYPES_INFO[0] = x;
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "bool",llvm_signature: "i1",
            layout: layout::one(),
            kind: PrimitiveKind::Bool
        }; PRIMITIVE_TYPES_INFO[1] = x;

        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "i8",llvm_signature: "i8",
            layout: layout::one(),
            kind: PrimitiveKind::SignedInt
        }; PRIMITIVE_TYPES_INFO[2] = x;
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "i16",llvm_signature: "i16",
            layout: layout::two(),
            kind: PrimitiveKind::SignedInt
        }; PRIMITIVE_TYPES_INFO[3] = x;
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "i32",llvm_signature: "i32",
            layout: layout::four(),
            kind: PrimitiveKind::SignedInt
        }; PRIMITIVE_TYPES_INFO[4] = x;
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "i64",llvm_signature: "i64",
            layout: layout::eight(),
            kind: PrimitiveKind::SignedInt
        }; PRIMITIVE_TYPES_INFO[5] = x;

        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "isize",llvm_signature: "i64",
            layout: layout::eight(),
            kind: PrimitiveKind::SignedInt
        }; PRIMITIVE_TYPES_INFO[6] = x;


        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "u8",llvm_signature: "i8",
            layout: layout::one(),
            kind: PrimitiveKind::UnsignedInt
        }; PRIMITIVE_TYPES_INFO[7] = x;
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "u16",llvm_signature: "i16",
            layout: layout::two(),
            kind: PrimitiveKind::UnsignedInt
        }; PRIMITIVE_TYPES_INFO[8] = x;
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "u32",llvm_signature: "i32",
            layout: layout::four(),
            kind: PrimitiveKind::UnsignedInt
        }; PRIMITIVE_TYPES_INFO[9] = x;
        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "u64",llvm_signature: "i64",
            layout: layout::eight(),
            kind: PrimitiveKind::UnsignedInt
        }; PRIMITIVE_TYPES_INFO[10] = x;

        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "usize",llvm_signature: "i64",
            layout: layout::eight(),
            kind: PrimitiveKind::UnsignedInt
        }; PRIMITIVE_TYPES_INFO[11] = x;

        let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "f16",llvm_signature: "half",
            layout: layout::two(),
            kind: PrimitiveKind::Decimal
        }; PRIMITIVE_TYPES_INFO[12] = x;
            let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "f32",llvm_signature: "float",
            layout: layout::four(),
            kind: PrimitiveKind::Decimal
        }; PRIMITIVE_TYPES_INFO[13] = x;
            let x: PrimitiveTypeInfo = PrimitiveTypeInfo::{
            name: "f64",llvm_signature: "double",
            layout: layout::eight(),
            kind: PrimitiveKind::Decimal
        }; PRIMITIVE_TYPES_INFO[14] = x;


        VOID_TYPE = &PRIMITIVE_TYPES_INFO[0];
        DEFAULT_BOOL_TYPE = &PRIMITIVE_TYPES_INFO[1];
        DEFAULT_INTEGER_TYPE = &PRIMITIVE_TYPES_INFO[9];
        DEFAULT_DECIMAL_TYPE = &PRIMITIVE_TYPES_INFO[13];
    }
    fn find_primitive_type(name: &i8, len: i32) : &PrimitiveTypeInfo{
        const idx: i32 = find_primitive_type_index(name, len);
        if idx == -1{
            return null;
        }
        return &PRIMITIVE_TYPES_INFO[idx];
    }
    fn find_primitive_type_index(name: &i8, len: i32) : i32{
        if len == 2{
            if mem::compare(name, "i8", 2) == 0{
                return 2;
            }
            if mem::compare(name, "u8", 2) == 0{
                return 7;
            }
        }
        if len == 3{
            if mem::compare(name, "i16", 3) == 0{
                return 3;
            }
            if mem::compare(name, "i32", 3) == 0{
                return 4;
            }
            if mem::compare(name, "i64", 3) == 0{
                return 5;
            }
            if mem::compare(name, "u16", 3) == 0{
                return 8;
            }
            if mem::compare(name, "u32", 3) == 0{
                return 9;
            }
            if mem::compare(name, "u64", 3) == 0{
                return 10;
            }
            if mem::compare(name, "f16", 3) == 0{
                return 12;
            }
            if mem::compare(name, "f32", 3) == 0{
                return 13;
            }
            if mem::compare(name, "f64", 3) == 0{
                return 14;
            }
        }
        if len == 4{
            if mem::compare(name, "void", 4) == 0{
                return 0;
            }
            if mem::compare(name, "bool", 4) == 0{
                return 1;
            }
        }
        if len == 5{
            if mem::compare(name, "usize", 5) == 0{
                return 11;
            }
            if mem::compare(name, "isize", 5) == 0{
                return 6;
            }
        }

        return -1;
    }
}
namespace output{
    pub enum LLVMInstructionKind{
        ReturnVoid, Unreachable, Empty,
        Label, JumpTo, Branch,
        StackAlloc, BinaryOp, Load, LoadIntegerValue, LoadNull, LoadBytes,
        Store, GetElementPtr, GetElementPtrArray, Cast, ReturnOp, Call,
    }
    pub struct LLVMInstruction{
        kind: LLVMInstructionKind,
        data: [u8; 32]
    }
    pub fn push_instructions(instructions: &vector::Vec<LLVMInstruction>, sym_table: &SymbolTable, output: &string::String){
        for x in 0 as u32 .. instructions.length{
            push_instruction(&instructions.array[x], sym_table, output);
        }
    }
    pub fn push_instruction(instruction: &LLVMInstruction, sym_table: &SymbolTable, output: &string::String){
        if instruction.kind == LLVMInstructionKind::ReturnVoid{
            string::append_str(output, "\tret void\n", 10);
        } else if instruction.kind == LLVMInstructionKind::Unreachable{
            string::append_str(output, "\tunreachable\n", 13);
        }else if instruction.kind == LLVMInstructionKind::Empty{
            string::append_str(output, "\tadd i32 0, 0\n", 14);
        }else if instruction.kind == LLVMInstructionKind::Label{
            const id: u32 = *(&instruction.data[0] as &u32);
            string::append(output, 'l');
            string::append_u64(output, id as u64);
            string::append_str(output, ":\n", 2);
        }else if instruction.kind == LLVMInstructionKind::JumpTo{
            const id: u32 = *(&instruction.data[0] as &u32);
            string::append_str(output, "\tbr label %l", 12);
            string::append_u64(output, id as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::Branch{
            const id: u32 = *(&instruction.data[0] as &u32);
            const thn: u32 = *(&instruction.data[4] as &u32);
            const els: u32 = *(&instruction.data[8] as &u32);

            string::append_str(output, "\tbr i1 %t", 9);
            string::append_u64(output, id as u64);
            string::append_str(output, ", label %l", 10);
            string::append_u64(output, thn as u64);
            string::append_str(output, ", label %l", 10);
            string::append_u64(output, els as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::StackAlloc{
            const x: &string::String = *(&instruction.data[8] as &&string::String);
            string::append_str(output, "\t%t", 3);
            string::append_u64(output, *(&instruction.data[0] as &u32) as u64);
            string::append_str(output, " = alloca ", 10);
            string::append_str(output, x.data, x.len);
            if *(&instruction.data[4] as &u32) != 4294967295{
                string::append_str(output, ", i32 %t", 8);
                string::append_u64(output, *(&instruction.data[4] as &u32) as u64);
            }
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::BinaryOp{
            const target: u32 = *(&instruction.data[0] as &u32);
            const src1: u32 = *(&instruction.data[4] as &u32);
            const src2: u32 = *(&instruction.data[8] as &u32);
            const op: &i8 = *(&instruction.data[16] as &&i8);
            const ty: &string::String = *(&instruction.data[24] as &&string::String);

            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = ", 3);
            
            string::append_str(output, op, string_utils::c_str_len(op));

            string::append_str(output, " ", 1);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, " %t", 3);
            string::append_u64(output, src1 as u64);
            string::append_str(output, ", %t", 4);
            string::append_u64(output, src2 as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::Cast{
            const target: u32 = *(&instruction.data[0] as &u32);
            const src: u32 = *(&instruction.data[4] as &u32);
            const op: &i8 = *(&instruction.data[8] as &&i8);
            const from_ty: &string::String = *(&instruction.data[16] as &&string::String);
            const to_ty: &string::String = *(&instruction.data[24] as &&string::String);

            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = ", 3);
            
            string::append_str(output, op, string_utils::c_str_len(op));

            string::append_str(output, " ", 1);
            string::append_str(output, from_ty.data, from_ty.len);
            string::append_str(output, " %t", 3);
            string::append_u64(output, src as u64);
            string::append_str(output, " to ", 4);
            string::append_str(output, to_ty.data, to_ty.len);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::Load{
            const target: u32 = *(&instruction.data[0] as &u32);
            const src: u32 = *(&instruction.data[4] as &u32);
            const ty: &string::String = *(&instruction.data[8] as &&string::String);

            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = load ", 8);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, ", ", 2);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, "* %t", 4);
            string::append_u64(output, src as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::LoadIntegerValue{
            const target: u32 = *(&instruction.data[0] as &u32);
            const val: u64 = *(&instruction.data[4] as &u64);
            const ty: &string::String = *(&instruction.data[12] as &&string::String);
            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = add ", 7);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, " ", 1);
            string::append_u64(output, val);
            string::append_str(output, ", 0", 3);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::LoadNull{
            const target: u32 = *(&instruction.data[0] as &u32);
            const ty: &string::String = *(&instruction.data[12] as &&string::String);
            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = inttoptr i64 0 to ", 21);
            string::append_str(output, ty.data, ty.len);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::LoadBytes{
            const target: u32 = *(&instruction.data[0] as &u32);
            const src: u32 = *(&instruction.data[4] as &u32);
            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = bitcast [", 12);
            string::append_u64(output, sym_table.symbol_vector.array[sym_table.constant_strings_used.array[src]].len as u64);
            string::append_str(output, " x i8]* @.d", 11);
            string::append_u64(output, src as u64);
            string::append_str(output, " to i8*", 7);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::Store{
            const target: u32 = *(&instruction.data[0] as &u32);
            const src: u32 = *(&instruction.data[4] as &u32);
            const ty: &string::String = *(&instruction.data[8] as &&string::String);

            string::append_str(output, "\tstore ", 7);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, " %t", 3);
            string::append_u64(output, src as u64);
            string::append_str(output, ", ", 2);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, "* %t", 4);
            string::append_u64(output, target as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::GetElementPtr{
            const target: u32 = *(&instruction.data[0] as &u32);
            const src: u32 = *(&instruction.data[4] as &u32);
            const element: u32 = *(&instruction.data[8] as &u32);
            const ty: &string::String = *(&instruction.data[16] as &&string::String);

            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = getelementptr inbounds ", 27);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, ", ", 2);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, "* %t", 4);
            string::append_u64(output, src as u64);
            string::append_str(output, ", i32 0, i32 ", 13);
            string::append_u64(output, element as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::GetElementPtrArray{
            const target: u32 = *(&instruction.data[0] as &u32);
            const src: u32 = *(&instruction.data[4] as &u32);
            const element: u32 = *(&instruction.data[8] as &u32);
            const ty: &string::String = *(&instruction.data[16] as &&string::String);

            string::append_str(output, "\t%t", 3);
            string::append_u64(output, target as u64);
            string::append_str(output, " = getelementptr inbounds ", 27);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, ", ", 2);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, "* %t", 4);
            string::append_u64(output, src as u64);
            string::append_str(output, ", i32 ", 6);
            string::append_u64(output, element as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::ReturnOp{
            const src: u32 = *(&instruction.data[0] as &u32);
            const ty: &string::String = *(&instruction.data[8] as &&string::String);

            string::append_str(output, "\tret ", 5);
            string::append_str(output, ty.data, ty.len);
            string::append_str(output, " %t", 3);
            string::append_u64(output, src as u64);
            string::append(output, '\n');
        }else if instruction.kind == LLVMInstructionKind::Call{
            const src: u32 = *(&instruction.data[0] as &u32);
            const ts: &string::String = *(&instruction.data[8] as &&string::String);
            const ta: &string::String = *(&instruction.data[16] as &&string::String);

            string::append_str(output, "\t%t", 3);
            string::append_u64(output, src as u64);
            string::append_str(output, " = call ", 8);
            string::append_str(output, ts.data, ts.len);
            string::append_str(output, "(", 1);
            string::append_str(output, ta.data, ta.len);
            string::append_str(output, ")\n", 2);
        }
    }
    pub fn ret_void() : LLVMInstruction {let x: LLVMInstruction; x.kind = LLVMInstructionKind::ReturnVoid; return x;}
    pub fn unreachable() : LLVMInstruction {let x: LLVMInstruction; x.kind = LLVMInstructionKind::Unreachable; return x;}
    
    pub fn label(label_id: u32) : LLVMInstruction {let x: LLVMInstruction; x.kind = LLVMInstructionKind::Label; *(&x.data[0] as &u32) = label_id; return x;}
    pub fn jump_to(label_id: u32) : LLVMInstruction {let x: LLVMInstruction; x.kind = LLVMInstructionKind::JumpTo; *(&x.data[0] as &u32) = label_id; return x;}
    pub fn branch(condition_register: u32, then_label: u32, else_label: u32) : LLVMInstruction {let x: LLVMInstruction; x.kind = LLVMInstructionKind::Branch; *(&x.data[0] as &u32) = condition_register; *(&x.data[4] as &u32) = then_label; *(&x.data[8] as &u32) = else_label; return x;}
    
    pub fn alloc_stack(target_reg: u32, count_register: u32, alloc_type_llvm_string: &string::String) : LLVMInstruction {let x: LLVMInstruction; x.kind = LLVMInstructionKind::StackAlloc;*(&x.data[0] as &u32) = target_reg; *(&x.data[4] as &u32) = count_register;*(&x.data[8] as &&string::String) = alloc_type_llvm_string;return x;}
    pub fn alloc(target_reg: u32, alloc_type_llvm_string: &string::String) : LLVMInstruction {let x: LLVMInstruction; x.kind = LLVMInstructionKind::StackAlloc;*(&x.data[0] as &u32) = target_reg; *(&x.data[4] as &u32) = 4294967295;*(&x.data[8] as &&string::String) = alloc_type_llvm_string;return x;}

    pub fn binary_operator(target_reg: u32, operation: &i8, type_llvm_string: &string::String, src_1_reg: u32, src_2_reg: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::BinaryOp;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u32) = src_1_reg;
        *(&x.data[8] as &u32) = src_2_reg;
        *(&x.data[16] as &&i8) = operation;
        *(&x.data[24] as &&string::String) = type_llvm_string;
        return x;
    }

    pub fn cast(target_reg: u32, operation: &i8, from_type_llvm_string: &string::String, to_type_llvm_string: &string::String, src_reg: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::Cast;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u32) = src_reg;
        *(&x.data[8] as &&i8) = operation;
        *(&x.data[16] as &&string::String) = from_type_llvm_string;
        *(&x.data[24] as &&string::String) = to_type_llvm_string;
        return x;
    }

    pub fn load(target_reg: u32, type_llvm_string: &string::String, src_reg: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::Load;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u32) = src_reg;
        *(&x.data[8] as &&string::String) = type_llvm_string;
        return x;
    }
    pub fn load_integer(target_reg: u32, type_llvm_string: &string::String, val: u64) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::LoadIntegerValue;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u64) = val;
        *(&x.data[12] as &&string::String) = type_llvm_string;
        return x;
    }
    pub fn load_null(target_reg: u32, type_llvm_string: &string::String) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::LoadNull;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u64) = 0;
        *(&x.data[12] as &&string::String) = type_llvm_string;
        return x;
    }
    pub fn load_bytes(target_reg: u32, stc_index: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::LoadBytes;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u32) = stc_index;
        return x;
    }
    pub fn store(target_reg: u32, type_llvm_string: &string::String, src_reg: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::Store;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u32) = src_reg;
        *(&x.data[8] as &&string::String) = type_llvm_string;
        return x;
    }

    pub fn get_element_ptr(target_reg: u32, type_llvm_string: &string::String, src_reg: u32, element: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::GetElementPtr;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u32) = src_reg;
        *(&x.data[8] as &u32) = element;
        *(&x.data[16] as &&string::String) = type_llvm_string;
        return x;
    }

    pub fn get_element_ptr_in_array(target_reg: u32, type_llvm_string: &string::String, src_reg: u32, element: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::GetElementPtrArray;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[4] as &u32) = src_reg;
        *(&x.data[8] as &u32) = element;
        *(&x.data[16] as &&string::String) = type_llvm_string;
        return x;
    }

    pub fn return_op(type_llvm_string: &string::String, src_reg: u32) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::ReturnOp;
        *(&x.data[0] as &u32) = src_reg;
        *(&x.data[8] as &&string::String) = type_llvm_string;
        return x;
    }
    pub fn call(target_reg: u32, call_site: &string::String, arguments: &string::String) : LLVMInstruction {
        let x: LLVMInstruction; x.kind = LLVMInstructionKind::Call;
        *(&x.data[0] as &u32) = target_reg;
        *(&x.data[8] as &&string::String) = call_site;
        *(&x.data[16] as &&string::String) = arguments;
        return x;
    }
}